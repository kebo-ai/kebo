import { View } from "react-native";

// This is a placeholder screen for the center FAB button
// The actual button is rendered in the tabs layout
// This screen should never actually be shown
export default function TransactionButton() {
  return <View />;
}
